/**
 * Raise a number to a power
 */

#include <cs50.h>
#include <stdio.h>

int main(void)
{
    // Prompt the user for a number
    double number = get_double("Give ma a number, please: ");

    // Prompt the user for a power
    double p = get_double("Give a power, please: ");
}