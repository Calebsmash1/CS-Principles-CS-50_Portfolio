/**
 * Converts Celsius into Farenheit
 */

#include <cs50.h>
#include <math.h>
#include <stdio.h>

int main(void)
{
    // Prompts the user for a temperature in Celsius
    int number1 = get_int("Integer: ");

    // Converts and prints user input into Farenheit
    printf("%i plus %i is %i\n", number1, number2, number1 + number2);


}